// satu
AFRAME.registerComponent("text-show-for-first", {
init: function() {
let toggle = true
  this.el.addEventListener("mouseenter", (e) => {
	let cylinders = document.querySelectorAll("#boxText1")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", toggle)
	}) })
  this.el.addEventListener("mouseleave", (e) => {
	let cylinders = document.querySelectorAll("#boxText1")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", !toggle)
	}) }) } })

	// dua
AFRAME.registerComponent("text-show-for-second", {
init: function() {
let toggle = true
  this.el.addEventListener("mouseenter", (e) => {
	let cylinders = document.querySelectorAll("#boxText2")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", toggle)

	})

  })

  this.el.addEventListener("mouseleave", (e) => {
	let cylinders = document.querySelectorAll("#boxText2")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", !toggle)

	})

  })

}
})

// tiga
 AFRAME.registerComponent("text-show-for-third", {
init: function() {
let toggle = true
  this.el.addEventListener("mouseenter", (e) => {
	let cylinders = document.querySelectorAll("#boxText3")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", toggle)

	})

  })

  this.el.addEventListener("mouseleave", (e) => {
	let cylinders = document.querySelectorAll("#boxText3")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", !toggle)

	})

  })

}
})


		 // empat
		   AFRAME.registerComponent("text-show-for-fourth", {
			init: function() {
			let toggle = true
			  this.el.addEventListener("mouseenter", (e) => {
				let cylinders = document.querySelectorAll("#boxText4")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", toggle)

				})

			  })

			  this.el.addEventListener("mouseleave", (e) => {
				let cylinders = document.querySelectorAll("#boxText4")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", !toggle)

				})

			  })

			}
		  })
		  // lima

		     AFRAME.registerComponent("text-show-for-fifth", {
			init: function() {
			let toggle = true
			  this.el.addEventListener("mouseenter", (e) => {
				let cylinders = document.querySelectorAll("#boxText5")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", toggle)

				})

			  })

			  this.el.addEventListener("mouseleave", (e) => {
				let cylinders = document.querySelectorAll("#boxText5")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", !toggle)

				})

			  })

			}
		  })
		  // enam

		     AFRAME.registerComponent("text-show-for-six", {
			init: function() {
			let toggle = true
			  this.el.addEventListener("mouseenter", (e) => {
				let cylinders = document.querySelectorAll("#boxText6")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", toggle)

				})

			  })

			  this.el.addEventListener("mouseleave", (e) => {
				let cylinders = document.querySelectorAll("#boxText6")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", !toggle)

				})

			  })

			}
		  })

		  // seven

		     AFRAME.registerComponent("text-show-for-seven", {
			init: function() {
			let toggle = true
			  this.el.addEventListener("mouseenter", (e) => {
				let cylinders = document.querySelectorAll("#boxText7")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", toggle)

				})

			  })

			  this.el.addEventListener("mouseleave", (e) => {
				let cylinders = document.querySelectorAll("#boxText7")
				cylinders.forEach(function(el) {
					el.setAttribute("visible", !toggle)

				})

			  })

			}
		  })

			//eight

			AFRAME.registerComponent("text-show-for-eight", {
	 init: function() {
	 let toggle = true
		 this.el.addEventListener("mouseenter", (e) => {
		 let cylinders = document.querySelectorAll("#boxText8")
		 cylinders.forEach(function(el) {
			 el.setAttribute("visible", toggle)

		 })

		 })

		 this.el.addEventListener("mouseleave", (e) => {
		 let cylinders = document.querySelectorAll("#boxText8")
		 cylinders.forEach(function(el) {
			 el.setAttribute("visible", !toggle)

		 })

		 })

	 }
	 })

	 //nine
	 AFRAME.registerComponent("text-show-for-nine", {
init: function() {
let toggle = true
	this.el.addEventListener("mouseenter", (e) => {
	let cylinders = document.querySelectorAll("#boxText9")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", toggle)

	})

	})

	this.el.addEventListener("mouseleave", (e) => {
	let cylinders = document.querySelectorAll("#boxText9")
	cylinders.forEach(function(el) {
		el.setAttribute("visible", !toggle)

	})

	})

}
})

	//ten
	AFRAME.registerComponent("text-show-for-ten", {
	init: function() {
	let toggle = true
	this.el.addEventListener("mouseenter", (e) => {
	let cylinders = document.querySelectorAll("#boxText10")
	cylinders.forEach(function(el) {
	 el.setAttribute("visible", toggle)

	})

	})

	this.el.addEventListener("mouseleave", (e) => {
	let cylinders = document.querySelectorAll("#boxText10")
	cylinders.forEach(function(el) {
	 el.setAttribute("visible", !toggle)

	})

	})

	}
	})

	//11
	AFRAME.registerComponent("text-show-for-eleven", {
init: function() {
let toggle = true
 this.el.addEventListener("mouseenter", (e) => {
 let cylinders = document.querySelectorAll("#boxText11")
 cylinders.forEach(function(el) {
	 el.setAttribute("visible", toggle)

 })

 })

 this.el.addEventListener("mouseleave", (e) => {
 let cylinders = document.querySelectorAll("#boxText11")
 cylinders.forEach(function(el) {
	 el.setAttribute("visible", !toggle)

 })

 })

}
})

//12
AFRAME.registerComponent("text-show-for-12", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText12")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText12")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

//13
AFRAME.registerComponent("text-show-for-13", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText13")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText13")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

//14
AFRAME.registerComponent("text-show-for-14", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText14")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText14")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

//15
AFRAME.registerComponent("text-show-for-15", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText15")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText15")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})
//16
AFRAME.registerComponent("text-show-for-16", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText16")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText16")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

//17
AFRAME.registerComponent("text-show-for-17", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText17")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText17")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})
//18
AFRAME.registerComponent("text-show-for-18", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText18")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText18")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

AFRAME.registerComponent("text-show-for-19", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText19")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText19")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

AFRAME.registerComponent("text-show-for-20", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText20")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText20")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

AFRAME.registerComponent("text-show-for-21", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText21")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText21")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

AFRAME.registerComponent("text-show-for-22", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText22")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText22")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

AFRAME.registerComponent("text-show-for-23", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText23")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText23")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})

AFRAME.registerComponent("text-show-for-24", {
init: function() {
let toggle = true
this.el.addEventListener("mouseenter", (e) => {
let cylinders = document.querySelectorAll("#boxText24")
cylinders.forEach(function(el) {
 el.setAttribute("visible", toggle)

})

})

this.el.addEventListener("mouseleave", (e) => {
let cylinders = document.querySelectorAll("#boxText24")
cylinders.forEach(function(el) {
 el.setAttribute("visible", !toggle)

})

})

}
})
